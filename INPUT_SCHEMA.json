{
  "title": "Bilibili Scraper Input",
  "description": "Input parameters for the Bilibili scraping actor.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "keywords": {
      "title": "Keywords",
      "type": "array",
      "description": "List of search keywords for Bilibili videos.",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": ["原神", "英雄联盟", "TechReview", "Vlog"]
      },
      "prefill": ["原神"]
    },
    "maxResults": {
      "title": "Max Results",
      "type": "integer",
      "description": "Maximum number of results to fetch.",
      "editor": "number",
      "default": 5
    },
    "includeComments": {
      "title": "Include Comments",
      "type": "boolean",
      "description": "Fetch top comments for each video.",
      "editor": "checkbox",
      "default": true
    }
  },
  "required": ["keywords"],
  "additionalProperties": false
}
